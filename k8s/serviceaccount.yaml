# Dedicated ServiceAccount — avoids inheriting default SA permissions
apiVersion: v1
kind: ServiceAccount
metadata:
  name: django-angular-main
  namespace: default
  labels:
    app.kubernetes.io/name: django-angular-main
    app.kubernetes.io/managed-by: devopsgen
automountServiceAccountToken: false  # Disable unless your app calls the K8s API
