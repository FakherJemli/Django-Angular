# Generated by DevOpsGen — PodDisruptionBudget
# Ensures at least 1 pod stays running during node drains and cluster upgrades.
# IMPORTANT: Only effective when replicaCount >= 2.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: django-angular-main
  namespace: default
  labels:
    app.kubernetes.io/name: django-angular-main
    app.kubernetes.io/managed-by: devopsgen
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: django-angular-main
  minAvailable: 1    # or use maxUnavailable: 1
