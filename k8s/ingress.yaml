# Generated by DevOpsGen
# ACTION REQUIRED:
#   1. Replace 'django-angular-main.example.com' with your actual domain
#   2. For AWS EKS (ALB), replace annotations with:
#        kubernetes.io/ingress.class: alb
#        alb.ingress.kubernetes.io/scheme: internet-facing
#   3. For GKE, replace with:
#        kubernetes.io/ingress.class: gce
#
# Install nginx ingress controller (if using nginx):
#   kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/\
#     controller-v1.10.1/deploy/static/provider/cloud/deploy.yaml
#
# To enable TLS with cert-manager, uncomment the tls section below and add:
#   cert-manager.io/cluster-issuer: letsencrypt-prod  to annotations
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-angular-main
  namespace: default
  labels:
    app.kubernetes.io/name: django-angular-main
    app.kubernetes.io/managed-by: devopsgen
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    # Uncomment for TLS:
    # cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  # Uncomment to enable TLS:
  # tls:
  #   - secretName: django-angular-main-tls
  #     hosts: [django-angular-main.example.com]
  rules:
    - host: django-angular-main.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: django-angular-main
                port:
                  number: 80
