# Generated by DevOpsGen — Non-sensitive configuration
# ⚠ NEVER put passwords, API keys, or tokens here.
#   Use Kubernetes Secrets for sensitive values.
#   For managed secret rotation, see: https://external-secrets.io
apiVersion: v1
kind: ConfigMap
metadata:
  name: django-angular-main-config
  namespace: default
  labels:
    app.kubernetes.io/name: django-angular-main
    app.kubernetes.io/managed-by: devopsgen
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  PORT: "8000"
  # Add your non-sensitive configuration below:
  # DATABASE_HOST: "your-db-host"
  # REDIS_HOST: "your-redis-host"
